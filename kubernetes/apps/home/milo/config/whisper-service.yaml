---
apiVersion: v1
kind: Service
metadata:
  name: whisper-transcription
  namespace: milo
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: api
  selector:
    app: whisper-transcription
