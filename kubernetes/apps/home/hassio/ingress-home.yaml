apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hassio
  namespace: home
spec:
  parentRefs:
  - name: envoy-external
    namespace: network
  hostnames:
  - home.gitlab.be
  rules:
  - backendRefs:
    - name: hassio-service
      port: 8123


