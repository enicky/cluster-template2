apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - http-routes.yaml
  - secret.sops.yaml
  - postgres-statefulset.yaml

patches:
  - target:
      kind: Secret
      name: healthtracking-db
    patch: |-
      - op: remove
        path: /sops
