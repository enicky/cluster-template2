controllers:
  backend:
    strategy: RollingUpdate
    containers:
      app:
        image:
          repository: ghcr.io/enicky/health-api
          tag: main-b254107
        env:
          ASPNETCORE_URLS: http://+:5001
          ASPNETCORE_ENVIRONMENT: Development
          DB_HOST: healthtracking-postgres
          DB_PORT: "5432"
          DB_NAME: healthtracking
          DB_USER: healthtracking
          ConnectionStrings__DefaultConnection: "Host=healthtracking-postgres;Port=5432;Database=healthtracking;Username=healthtracking;Password=$(DB_PASSWORD)"
          Logging__LogLevel__Default: Information
        envFrom:
          - secretRef:
              name: healthtracking-db
        ports:
          - name: http
            containerPort: 5001
            protocol: TCP
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path:  /health/live
                port: 5001
              initialDelaySeconds: 30
              periodSeconds: 10
              timeoutSeconds: 5
              failureThreshold: 3
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /health/ready
                port: 5001
              initialDelaySeconds: 10
              periodSeconds: 5
              timeoutSeconds: 3
              failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          capabilities: { drop: ["ALL"] }
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi

  frontend:
    strategy: RollingUpdate
    containers:
      app:
        image:
          repository: ghcr.io/enicky/health-frontend
          tag: main-b254107
        env:
          NODE_ENV: production
          VITE_API_URL: https://health.gitlab.be/api
        ports:
          - name: http
            containerPort: 5173
            protocol: TCP
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /
                port: 5173
              initialDelaySeconds: 30
              periodSeconds: 10
              timeoutSeconds: 5
              failureThreshold: 3
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /
                port: 5173
              initialDelaySeconds: 10
              periodSeconds: 5
              timeoutSeconds: 3
              failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          capabilities: { drop: ["ALL"] }
          readOnlyRootFilesystem: false
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi

defaultPodOptions:
  securityContext:
    runAsNonRoot: false
    fsGroup: 999

service:
  backend:
    controller: backend
    ports:
      http:
        port: 5001
        targetPort: 5001

  frontend:
    controller: frontend
    ports:
      http:
        port: 5173
        targetPort: 5173
