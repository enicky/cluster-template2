apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sprinkler
  namespace: home
spec:
  parentRefs:
  - name: envoy-external
    namespace: network
  hostnames:
  - sprinkler.gitlab.be
  rules:
  - backendRefs:
    - name: sprinkler-service
      port: 80


