apiVersion: v1
kind: Endpoints
metadata:
  name: unifi-service
  namespace: home
subsets:
- addresses:
  - ip: #{ bootstrap_apps.unifi.host }#
  ports:
  - port: #{ bootstrap_apps.unifi.port }#
    name: http
    protocol: TCP
